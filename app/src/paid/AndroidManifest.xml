<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity android:name="com.rahulahuja.gradleexplained.paid.PaidActivity">
            <intent-filter>
                <action android:name="flavor.paid.PaidActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
